{
  "name": "descontocemig",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:client && node scripts/prepare-public.js",
    "build:client": "esbuild src/cadastro.js --bundle --format=esm --platform=browser --target=es2020 --outfile=cadastrar_conta/cadastro.js --minify"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
